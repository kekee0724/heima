server:
  port: 80

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/reco_mini #?serverTimezone=UTC
    username: kekee
    password: "0724"
  main:
    banner-mode: off
#开启mp的日志（输出到控制台）
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: false
    db-config:
      id-type: auto
      table-prefix: bi_
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0